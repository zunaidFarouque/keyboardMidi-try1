{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Debug OmniKey (Windows)",
            "type": "cppvsdbg",
            "request": "launch",
            // This asks CMake where the exe is automatically
            "program": "${command:cmake.launchTargetPath}",
            "args": [],
            "stopAtEntry": false,
            "cwd": "${workspaceFolder}",
            "environment": [],
            "console": "externalTerminal"
        }
    ]
}